stages:
  - test

default:
  interruptible: true
  retry: 2

include:
  - template: "Code-Quality.gitlab-ci.yml"

code_quality:
  variables:
    REPORT_FORMAT: html
  artifacts:
    paths: ["gl-code-quality-report.html"]
